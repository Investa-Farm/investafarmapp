- [TOTP MFA implementation](totp-mfa.md) — otplib v13: use globalThis.require (from ESM banner) NOT createRequire in compiled bundle; add `.default` fallback: `const otplib = rawOtplib?.default ?? rawOtplib`; verifySync returns `{valid:bool}` not bool; generateSecret/Sync/URI all take object payloads
- [Admin session token auth](admin-session-token.md) — requireAdmin accepts EITHER: Bearer JWT (role=admin) OR base64 admin-session token from /api/admin/login; /admin/login now also accepts email+bcrypt for individual admin accounts created via /admin/create-admin; dashboard uses sessionStorage admin_token as auth header
- [Cooperative role in DB](cooperative-role.md) — DB schema originally only had "farmer"|"investor"; "cooperative" added to `userRoleEnum` in `lib/db/src/schema/users.ts` and pushed via `pnpm --filter @workspace/db run push`
- [Orval codegen config](orval-config.md) — Do NOT re-run codegen without checking `mode: "single"` in orval config; output must target `generated/api.ts` (no schemas folder)
- [Crop image fallback pattern](crop-images.md) — Market pages use `getCropImage(cropType, imageUrl?)` in `artifacts/investa-farm/src/lib/crops.ts`; now uses Pexels photos imported via @assets alias for beans/tomatoes/dairy/poultry/rice/kale/sunflower/cabbage/greenhouse
- [OTP + email verification flow](otp-email-flow.md) — AuthGuard blocks unverified users (emailVerified===false → redirect to /verify-otp); login returns 403+tempToken+requiresOtp if unverified and resends OTP; login.tsx uses raw fetch() not useLogin hook
- [Weekly email scheduler](weekly-scheduler.md) — node-cron scheduler in artifacts/api-server/src/scheduler.ts; runs Mon 8am & Fri 6pm EAT (Africa/Nairobi timezone); calls sendOpportunityDigest in email.ts; started in index.ts after seed
- [Port conflict fix](port-conflict.md) — When restarting API Server fails with EADDRINUSE on port 8080, run `fuser -k 8080/tcp` before restarting the workflow
- [Agribusiness role + notifications](agribusiness-notifications.md) — "agribusiness" role added with sub-types (farmer_connector/input_supplier); notificationsTable created and pushed; notifications API at /api/notifications; AuthGuard uses (user as any)?.role cast to avoid TS type overlap errors
- [KYC all-docs-first pattern](kyc-upload-pattern.md) — KYC modals use popup overlay (UploadPopup component via createPortal) not inline forms; selfie uses MediaDevices getUserMedia; upload button only shown after file staged; "under review" email+notification sent when all required docs are uploaded
- [Risk level badges](risk-levels.md) — getRiskLevel(cropType, changePercent) helper computes Low/Moderate/High risk; RiskBadge component used in market/index.tsx, market/primary.tsx; high-risk crops: coffee, avocado, tobacco; moderate: tea, wheat, tomatoes
- [Hero header grass green](hero-header-colors.md) — hero-header CSS is grass green #052e16→#14532d→#16a34a; dark mode uses #052e16→#14532d→#166534; was previously slate, reverted to green per user request
- [Web push notifications](web-push-setup.md) — web-push pkg in api-server; VAPID keys in VAPID_PUBLIC_KEY/VAPID_PRIVATE_KEY env vars; push_subscriptions table pushed to DB; initVapid() called in index.ts; notifyUser() helper in push.ts sends both in-app+push; sw.js registered in dev+prod
- [Farmer contract flow](farmer-contract.md) — loan-apply.tsx: contract modal (scroll-to-bottom + checkbox required before form); on submit, sendFundingApplicationEmail() fires (non-critical, .catch ignored); email includes AI score + key contract terms
- [Farmer wallet popup](farmer-wallet-popup.md) — farmer/profile.tsx uses WalletModal (same as investor) not /farmer/wallet page; walletOpen state, "Open Wallet" button triggers it; /farmer/wallet route still exists as fallback
- [Secondary market crop filter](secondary-crop-filter.md) — cropFilter state in secondary.tsx; horizontal chip row auto-builds from unique cropTypes; listings filtered client-side; broker 1% fee badge (violet) on each listing card
- [Currency context pattern](currency-context.md) — USD is default; CurrencyProvider wraps app in App.tsx; useCurrency() returns formatAmount(kesValue), toDisplay(kesValue), setCurrency(code); rates stored in CURRENCIES array in src/lib/currency.tsx; preference persisted in localStorage("investa_currency")
- [Market header redesign](market-header-light.md) — market/index.tsx hero-header replaced with bg-background border-b light design; ticker uses text-foreground/muted colors; stat cards use bg-primary/5; calc/alert icon buttons beside BUY button; news section fetches from /api/news (Groq AI + static fallback)
- [Price alerts + news API](price-alerts-news.md) — priceAlertsTable pushed to DB; /api/price-alerts CRUD; /api/news uses Groq llama-3.3-70b-versatile with 1hr cache + 6-item static fallback; InvestmentCalculator and PriceAlertModal bottom sheet components in src/components/
- [Platform fee structure](platform-fees.md) — 1.5% primary purchase fee, 0.5% secondary trade fee (both applied in market.ts buy route); 0.5% withdrawal fee capped KES 260 in wallet.ts; transactionFeesTable in lib/db/src/schema/transaction_fees.ts; API server must be rebuilt with `node ./build.mjs` after route changes
- [AI explain micro-bots](ai-explain-bots.md) — AiSectionBot component at src/components/ai-section-bot.tsx; uses /api/ai/explain POST (raw fetch to Groq, 2h in-memory cache); existing routes use raw fetch to Groq API NOT groq-sdk (groq-sdk is not in build externals)
- [Weather + NDVI on farm detail](weather-ndvi.md) — WeatherNdvi component embedded in farm-detail.tsx Growth tab; calls Open-Meteo API directly from frontend (no API key); NDVI computed from crop type + growth stage via NDVI_BY_STAGE lookup table; lat/lng from existing getKenyaCoords() function
- [VerifyBanner resend OTP](verify-banner.md) — VerifyBanner in App.tsx uses useState for dismiss state (localStorage alone doesn't trigger re-render); Resend button calls POST /api/auth/send-otp; App.tsx needed both useLocation AND useState imports from wouter/react
- [WalletModal farmer role check](wallet-farmer-role.md) — WalletModal hides "Add Funds" button for farmers using (user as {role?:string}|null)?.role === "farmer" cast; farmer/wallet.tsx page has "Withdraw only" correctly; farmer/profile.tsx uses WalletModal with no-deposit behavior
- [Reinvestment automation](reinvestment-automation.md) — reinvestment_rules table (one row per user, unique userId); API at /api/reinvestment/rule (GET/POST) and /api/reinvestment/simulate (POST); ReinvestmentSettings bottom-sheet in portfolio.tsx; DB pushed June 2026
- [DCF pricing engine](dcf-pricing.md) — full no-arb DCF in market.ts listingToJson AND scheduler.ts; P_fair=(α×R̂/N)×PV×(1-λ); secondary adds imbalance β=0.10, δ=0.005; response includes dcfFairValue+dcfAskPrice+dcfPremiumPct; frontend shows "vs DCF" badge in secondary, "DCF Fair Value" in primary expanded card
- [Dividends system](dividends-system.md) — dividendsTable in lib/db/src/schema/dividends.ts; payout scheduler runs daily 2am EAT; triggerFarmHarvest() exported from scheduler.ts for admin manual trigger; admin routes /admin/farms/:id/harvest and /admin/dividends
- [Investor feed + farmer vouchers](investor-feed-vouchers.md) — GET /api/investor/feed returns farm_updates for investor's active farms; GET /api/farmer/vouchers returns approved loans with IFV-{id}-{farmerId} codes; both in artifacts/api-server/src/routes/investor-feed.ts
- [Activity farm feed tab](activity-feed.md) — activity.tsx has 3 tabs: Transactions, Placements, Farm Feed; feed tab fetches /api/investor/feed enabled only when active
- [Broker unlock popup](broker-unlock-popup.md) — portfolio.tsx shows 3-step bottom sheet when qualification.qualified becomes true and localStorage investa_broker_unlocked not set; step 0=congrats, 1=benefits, 2=activate; activating sets broker_mode+unlocked flags
- [Admin farms+payouts tabs](admin-farms-payouts.md) — admin dashboard has 6 tabs now: overview/users/kyc/txns/farms/payouts; farms tab shows funding progress + harvest trigger; payouts tab shows dividends history with totalPaid summary
- [Order book + matching engine](order-book.md) — order_book table via raw SQL; /api/orders (POST/GET mine/DELETE); /api/orders/book/:farmId for depth; matching engine runs every 2min via cron in scheduler.ts; secondary.tsx has 3-tab layout (All Listings / Order Book / Mine)
- [Stellar custodial wallets](stellar-custodial.md) — stellar_accounts table; AES-256 encrypted keypairs; IFV-XXXX-XXXX account numbers; /api/stellar/account (GET); investors never see G... address; portfolio.tsx shows dark card with account number
- [Price alert watcher toast](price-alert-toast.md) — PriceAlertWatcher component in src/components/price-alert-watcher.tsx; polls /api/notifications every 60s; shows Sonner toasts for new price_alert type notifications; mounted in App.tsx; Sonner Toaster must import from "sonner" directly (NOT @/components/ui/sonner which uses next-themes useTheme and crashes)
- [Secondary market AnimatePresence fix](secondary-animatepresence.md) — secondary.tsx uses boolean && conditionals inside AnimatePresence (NOT ternary a ? b : c ? d : null); 3-way ternary in JSX AnimatePresence caused Babel parser error "Unexpected token expected ':'"
- [Demo data gate](demo-gate.md) — Mock data (TICKER_ITEMS, BUYER_OFFERS, etc.) shown ONLY for demo emails via isDemoAccount(); real users get live API data from /api/market/ticker and /api/farmer/market/buyers.
- [Live market ticker](market-ticker.md) — GET /api/market/ticker in market.ts; aggregates changePercent from farmsTable by cropType + COMMODITY_DEFAULTS for missing crops; no auth needed; 5min staleTime on frontend.
- [Paystack webhook](paystack-webhook.md) — POST /api/wallet/paystack/webhook in wallet.ts; HMAC-SHA512 on x-paystack-signature header; idempotency check via walletTransactionsTable.reference; credits wallet on charge.success.
- [Farmer buyer marketplace](farmer-marketplace.md) — GET /api/farmer/market/buyers derives buyers from farmer's farmTable cropTypes via BUYER_CATALOG; POST /api/farmer/market/connect stores notificationsTable row; POST /api/farmer/market/crop-proposal creates pending farm.
- [Stellar issuer keypair](stellar-issuer.md) — STELLAR_ISSUER_SECRET_KEY in env; getIssuerKeypair() + fundUserAccount() added to stellar.ts; testnet; never expose secret to frontend.
- [News API priority chain](news-sentiment.md) — TheNewsAPI→Currents→GNews→Mediastack→RSS→Static; GNEWS_API_KEY+CURRENTS_API_KEY+THENEWSAPI_TOKEN from env; Bluesky public API (no key); /api/news + /api/news/sentiment
- [Crop sentiment engine](news-sentiment.md) — Groq llama-3.3-70b-versatile returns per-crop score -100→+100; fallback uses BASE_SENTIMENT map + article scan; sentimentScoresTable in DB; displayed as strip in market news section
- [Modal z-index rule](modal-zindex.md) — bottom nav is z-50; ALL modals/bottom-sheets must use z-[60]+ to render above it; CommitFunds sheet in market/index.tsx and InvestModal both use z-[60]; stripHtml in news.ts must double-pass (strip→decode entities→strip) to handle Google News HTML-encoded RSS
- [Rainfall impact engine](rainfall-roi.md) — getRainfallData() in api-server/src/lib/rainfall.ts fetches Open-Meteo (no API key); GET /api/farms/:id/rainfall; computes rainfallFactor 0–1 per crop; shown in farm-detail.tsx Growth tab as color-coded card
- [ROI calculation engine](rainfall-roi.md) — computeROI() in api-server/src/lib/roi.ts; GET /api/portfolio/roi (auth required); full-season (dividend DCF) + mid-season (secondary P_sell DCF); portfolio.tsx holding cards show real AI ROI projections
- [Daily ROI + rainfall schedulers](rainfall-roi.md) — runDailyRoiSnapshots() + runRainfallAlerts() added to scheduler.ts; roi_projections table stores daily snapshots; rain alerts notify investors at 6am EAT; MEDIASTACK_API_KEY in shared env for /api/news
- [Agribusiness portal pages](agribusiness-pages.md) — /agribusiness/network → pages/agribusiness/network.tsx (farmer list, search/filter, KYC status, commission summary); /agribusiness/profile → pages/agribusiness/profile.tsx (business details edit, KYC docs, stats); dashboard nav tabs now navigate to these pages via Link
- [Wealth fund manager dashboard](fund-manager-dashboard.md) — /market/fund → pages/market/fund-dashboard.tsx (indigo theme, AUM, farm portfolio, client allocations, analytics); /market route auto-redirects fund_manager type investors via localStorage check; investor type set at registration via InvestorTypeModal
- [Sparkline deterministic seeding](sparkline-seeding.md) — generateSparkData() now uses seededRandom(Math.round(base*100)) so same farm always shows same chart; accepts positive prop for auto color; `width` prop added; previously used Math.random() causing chart to jump on re-render
- [Farm map coordinate fallback](farm-map-coords.md) — resolveCoords() in farm-map.tsx falls back to Nairobi [-1.2921, 36.8219] for unrecognised locations instead of random scatter; LOCATION_COORDS table covers 17 major Kenya regions
- [Primary market ROI formula](primary-roi-formula.md) — getTargetRoi() uses changePercent (from API) not id%7 for offset; generateSeasonHistory() uses annualised trend from changePercent instead of fixed linear ramp
