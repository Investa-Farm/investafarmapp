kind = "web"
previewPath = "/"
title = "Investa Farm"
version = "1.0.0"
id = "artifacts/investa-farm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19899

[services.development]
run = "pnpm --filter @workspace/investa-farm run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/investa-farm", "run", "build" ]
publicDir = "artifacts/investa-farm/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19899"
BASE_PATH = "/"
